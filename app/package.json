{
  "name": "mhi-progress",
  "version": "0.1.0",
  "private": true,
  "description": "MHI 進捗管理支援システム（部品進捗）— NestJS + Prisma + React(Vite) / PostgreSQL",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "setup": "cd backend && npm run prisma:generate && npm run prisma:deploy && npm run seed && npm run etl",
    "dev:api": "cd backend && npm run start:dev",
    "dev:web": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "typecheck": "cd backend && npm run typecheck && cd ../frontend && npm run typecheck",
    "selftest": "cd backend && npm run selftest"
  }
}
